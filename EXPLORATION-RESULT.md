# Exploration Results

| Candidate | Is the app usable? | Does front-end have upgrade potential? | Does back-end have upgrade potential? | Does current app mod work with front-end? | Does current app mod work with back-end? | Complexity (1-10) to execute DEMO-README |
|---|---|---|---|---|---|---|
| **mehmetpekdemir-Todo-App** | ✅ Yes — Backend (8085) serves Swagger UI, frontend (3004) renders "Todo App". Signup, login, and CRUD all work. | ✅ Yes — React 17, react-scripts 4.0.2, axios 0.21.1 (known CVEs), bootstrap 4.6, reactstrap 8.9, jwt-decode 3.1.2. All 2–3 major versions behind. | ✅ Yes — Java 11 → 21 and Spring Boot 2.2.6 → 3.x. Covers Jakarta namespace migration, springfox→springdoc, jjwt 0.9→0.12. Large, compelling upgrade. | TBD | TBD | **4** — Docker MySQL + Maven build + npm start. Straightforward. One external dep (MySQL via Docker). Clear instructions. |
| **didinj-spring-boot-mongodb-react-java-crud** | ❌ No — Neither backend nor frontend is reachable. The Gradle build process is suspended. The app never started successfully. | ✅ Yes — React 16.2, react-scripts 1.1.1, axios 0.18.0. The oldest frontend of all candidates. Massive upgrade potential (5+ major versions behind on react-scripts). | ✅ Yes — Java 8 → 21 and Spring Boot 2.0.0 → 3.x. The oldest backend. Uses deprecated Gradle `compile` keyword. Most dramatic upgrade story. | TBD | TBD | **7** — Requires Java 8 Temurin (not commonly installed), MongoDB via Docker, Gradle 4.5 (incompatible with Java 17+). The build failed in practice. Known code bugs (Edit.js sends wrong field). |
| **wesleyfuchter-todolist-spring-react** | ✅ Yes — Backend (8090) returns task JSON, frontend (3010) renders React app. Can create/complete/view todos. | ✅ Yes — React 17, react-scripts 4.0.3, Material UI 4.12.3 (→ MUI 5 is a major rename + API change), react-router-dom 5.3 (→ 6 is a complete rewrite). Good upgrade surface. | ✅ Yes — Java 11 → 21 and Spring Boot 2.5.5 → 3.x. Clean Jakarta migration path. Flyway needs version bump for SB3 compatibility. | TBD | TBD | **5** — Requires PostgreSQL (Docker or Homebrew) plus manual `psql` commands to create DB/user/grants. More setup steps than MySQL-based candidates. |
| **RameshMF-ReactJS-Spring-Boot-CRUD-Full-Stack-App** | ✅ Yes — Backend (8084) returns employee JSON (H2 in-memory), frontend (3005) renders React app. Full CRUD (add/view/edit/delete employees) works. | ✅ Yes — React 16.13.1, react-scripts 3.4.1, axios 0.19.2 (CVEs!), bootstrap 4.5, react-router-dom 5.2. All very old. `npm audit` shows 220 vulnerabilities (16 critical). Most compelling frontend-upgrade story. | ❌ No — Already on Java 17 + Spring Boot 3.0.4. Backend is essentially modern. Nothing meaningful to upgrade. | TBD | TBD | **2** — No external database (H2 in-memory). Just `mvn package` + `npm start`. Simplest of all candidates. |
| **GSkoba-spring-react-app** | ✅ Yes — Backend (8082) returns 3 seed todos, frontend (3002) renders todo list. Can add and delete todos. | ✅ Yes — React 16.13.1, react-scripts 3.4.1, TypeScript ~4.0 (originally 3.7). Old but the TS dimension adds upgrade interest (TS 3.7→5.x). Fewer npm deps overall though. | ✅ Yes — Java 8 → 21 and Spring Boot 2.2.6 → 3.x. Gradle 6.3 → 8.x. Deprecated `compile` → `implementation`. Strong upgrade story. | TBD | TBD | **5** — Requires either MongoDB or manual code edits to enable in-memory mode. Gradle 6.3 is incompatible with Java 17+ (must use JDK ≤14 or upgrade Gradle first). DEMO-README includes manual Java code edits. |
